# 광고심의 OCR (RapidOCR + 한국어 PP-OCRv5) — Python 3.12 / Ubuntu 24.04 / amd64
# ★ headless 서버 함정: rapidocr는 opencv-python(full)을 끌어오는데 headless 서버엔
#   libGL.so.1 이 없어 import cv2 가 실패한다. → opencv-python-headless 로 교체
#   (빌드 스크립트/Dockerfile 이 opencv-python 을 제거하고 이걸 남긴다).
rapidocr==3.8.4
onnxruntime==1.27.0
opencv-python-headless==4.13.0.92
# 스캔 PDF 래스터화(관리자 사규 적재 OCR) — 페이지 이미지화 후 RapidOCR
pymupdf==1.27.2.3
numpy==2.4.6
pillow==12.2.0
pyclipper==1.4.0
shapely==2.1.2
six==1.17.0
